import React from 'react';
import PaymentAmount from './PaymentAmount';

const PaymentAmountScreen = ({ navigation }) => (
  <PaymentAmount navigation={navigation} />
);

export default PaymentAmountScreen;
