import React from 'react';
import TransferReason from './TransferReason';

const TransferReasonScreen = ({ navigation }) => (
  <TransferReason navigation={navigation} />
);

export default TransferReasonScreen;
