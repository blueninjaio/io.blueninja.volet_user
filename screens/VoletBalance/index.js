import React from 'react';
import VoletBalance from './VoletBalance';

const VoletBalanceScreen = ({ navigation }) => (
  <VoletBalance navigation={navigation} />
);

export default VoletBalanceScreen;
