import React from 'react';
import TransferSummary from './TransferSummary';

const TransferSummaryScreen = ({ navigation }) => (
  <TransferSummary navigation={navigation} />
);

export default TransferSummaryScreen;
