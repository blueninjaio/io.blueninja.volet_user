import React from "react";
import FromAgentCollection from "./FromAgentCollection";

const FromAgentCollectionScreen = ({ navigation }) => (
  <FromAgentCollection navigation={navigation} />
);

export default FromAgentCollectionScreen;
