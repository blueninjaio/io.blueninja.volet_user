import React from "react";
import RequestPayment from "./RequestPayment";

const RequestPaymentScreen = ({ navigation }) => (
  <RequestPayment navigation={navigation} />
);

export default RequestPaymentScreen;
