import React from "react";
import TransactionDetails from "./TransactionDetails";

const TransactionDetailsScreen = ({ navigation }) => (
  <TransactionDetails navigation={navigation} />
);

export default TransactionDetailsScreen;
